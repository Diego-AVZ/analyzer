{
  "name": "binance-correlation-analyzer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run build-frontend",
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts",
    "frontend": "cd frontend && npm start",
    "build-frontend": "cd frontend && npm run build",
    "server": "npm run build && node backend-server.js",
    "dev-server": "npm run build && nodemon backend-server.js",
    "vercel-build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Diego-AVZ/analyzer.git"
  },
  "author": "diego-avz",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Diego-AVZ/analyzer/issues"
  },
  "homepage": "https://github.com/Diego-AVZ/analyzer#readme",
  "description": "Analizador de correlaciones inversas entre tokens de Binance",
  "dependencies": {
    "axios": "^1.6.0",
    "node-beep": "^0.0.3",
    "ws": "^8.18.3",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
